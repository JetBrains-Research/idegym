<idea-plugin>
    <id>com.idegym.open-project</id>
    <name>IdeGYM Open Project</name>
    <vendor>JetBrains</vendor>
    <description>
        Opens the project at the path given by the IDEGYM_PROJECT_ROOT environment variable
        when the IDE starts up. If a project is already open (e.g., passed via the command
        line), this listener is a no-op.
    </description>

    <!-- Depends only on the platform module so it works in all IntelliJ-based IDEs. -->
    <depends>com.intellij.modules.platform</depends>

    <applicationListeners>
        <listener class="com.idegym.OpenProjectOnStartup"
                  topic="com.intellij.ide.AppLifecycleListener"/>
    </applicationListeners>
</idea-plugin>
