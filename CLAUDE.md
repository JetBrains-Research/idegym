# CLAUDE.md

The repository's conventions, invariants, and coding standards live in
[`AGENTS.md`](AGENTS.md). Read it before planning a change, and update it as part of any
change that makes it inaccurate — see its "Keeping this file true" section.

This file exists only so that Claude Code loads those rules automatically; keep the content
itself in `AGENTS.md` so every agent and every human reads the same document.
