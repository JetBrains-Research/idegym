"""The authoritative OpenHands tools runtime: catalog, scheduler, artifacts, terminals, adapters.

Everything under this package is runtime-only (installed via ``idegym-openhands-tools[service]``)
and must never be imported by the client operations plugin.
"""
